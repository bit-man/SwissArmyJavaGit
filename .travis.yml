language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
before_install:
  - git config --global user.email "you@example.com"
  - git config --global user.name "Your Name"
script:
  - cd javagit
  - mvn install
